#from django import forms;
from .models import Post;

#class MainSearchForm(forms.Form):               # упрощенная форма на страницы списка новостей (с чего начинается сам поиск)
#    query_string = forms.CharField();
#    class Meta:
#        model = Post;
#        fields = [header, text];